module.exports = {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Keep autoprefixer if using standard next.js setup, though v4 might handle it. Safe to keep.
    },
}
